import React from 'react';
import Wrapper from '../assets/Styles/LoaderWrapper';
const Loader = () => {
  return (
    <Wrapper className="loader-container">
      <div className="loader"></div>
    </Wrapper>
  );
};

export default Loader;